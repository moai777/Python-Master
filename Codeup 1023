<<Me>>

a = float(input('5.7'))
print(int(a))
print(a - int(a))

모호르겠다~





<<Answer>>

a,b = input().split('.')
print(int(a))
print(int(b))





<<Explanation>>

The "valueerror: too many values to unpack (expected 2)"
error occurs when you do not unpack all the items in a list.


def getNumbers():
    return 'one', 'two', 'three'
one, two = getNumbers()

(x)
 
def getNumbers():
    return 'one', 'two'
one, two = getNumbers()

(o)


<<p.s>>
